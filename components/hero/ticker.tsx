"use client";

import { useState } from "react";
import { Button } from "@nextui-org/button";

export const Ticker = () => {
  const [count, setCount] = useState(0);

  return (
    <section></section>
  );
};
